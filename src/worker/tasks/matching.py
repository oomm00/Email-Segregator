"""Matching task — placeholder for Phase 4.

Will consume match.found events and execute matching logic
against accounts, ports, and known entities.
"""
import logging

logger = logging.getLogger(__name__)


def placeholder():
    logger.info("matching service placeholder — Phase 4 implementation pending")
